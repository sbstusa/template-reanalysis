%%% Common Definitions to be used by both paper and presentation %%%

% this next line assumes we are working in the paper or presentation dirs
\input{../vc.tex}

%%% Title, dates, other metadata
%%% Author is set in the paper.tex and presentation.tex because one uses
%%% \thanks and the other uses \institute

% we define a variable to hold the title so we can use it in the data appendix
% (or other supplemental docs)
\newcommand{\ourtitle}{Ethnicity and Electoral Fraud in New Democracies: \\
  Modelling Political Party Agents in Ghana}
%  Modelling Voter Registration Irregularities in Ghana}
%\newcommand{\ourtitle}{Ethnicity and Corruption in New Democracies: \\
%  The case of registration rigging in Ghana}
\title{\ourtitle}

\date{\today ~(Version:~\GITAbrHash)}

%%% Packages %%%
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage[round]{natbib}
\usepackage{url}
\usepackage{listings}
\usepackage{float} % for the H placement of figures and tables
\usepackage[english]{babel}

% next line taken from presentation.Rnw
% if we migrate something to the paper and it needs a package, look here:
% \usepackage{array,upquote,amsmath,nccmath,booktabs,makeidx,multirow}

\usepackage{epigraph}
\renewcommand{\epigraphsize}{\footnotesize}
\setlength{\epigraphrule}{0pt}
\setlength{\epigraphwidth}{4in}

%%% Figures and Tables %%%
\graphicspath{{.}{../figures/}} % again, path relative to paper/ and pres/
% \usepackage[font=small]{caption}
% \usepackage{subcaption} MMF: this causes a conflict with sciposter

% Possessive citation
% (http://www.economics.utoronto.ca/osborne/latex/BIBTEX.HTM):
\def\citeapos#1{\citeauthor{#1}'s (\citeyear{#1})}

%%% Convenient mathematical notation
\usepackage{../styles/notation}

%% These next lines tell latex that it is ok to have a single graphic
%% taking up most of a page, and they also decrease the space around
%% figures and tables.
\renewcommand\floatpagefraction{.9}
\renewcommand\topfraction{.9}
\renewcommand\bottomfraction{.9}
\renewcommand\textfraction{.1}   
\setcounter{totalnumber}{50}
\setcounter{topnumber}{50}
\setcounter{bottomnumber}{50}
\setlength{\intextsep}{2ex}
\setlength{\floatsep}{2ex}
\setlength{\textfloatsep}{2ex}

%\usepackage[usenames,dvipsnames]{color}
%\usepackage{xcolor}
%\usepackage[xetex,colorlinks=TRUE,linkcolor=BrickRed,citecolor=MidnightBlue]{hyperref}

